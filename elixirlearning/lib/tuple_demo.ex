defmodule TupleDemo do
  @moduledoc false


  #tuples are like fixed length arrays
  # Contigous memory allocation
  # use in scenarios where the information stored will not be changed often
  # use it to hold one piece of information, not a list like return data from a function or keyword lists
  # adding and removal from tuple is costly as they actually create a new tuple with new set of memory allocated
  def testTuple() do



  end


end
